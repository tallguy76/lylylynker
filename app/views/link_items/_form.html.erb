<%= form_for(@link_item) do |f| %>
  <% if @link_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@link_item.errors.count, "error") %> prohibited this link_item from being saved:</h2>

      <ul>
      <% @link_item.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :original_url %><br>
    <%= f.text_field :original_url %>
  </div>
  <div class="field">
    <%= f.label :short_name %><br>
    <%= f.text_field :short_name %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.text_field :password %>
  </div>
  <div class="field">
    <%= f.label :needs_email %><br>
    <%= f.check_box :needs_email %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
